# Setup environment

Bash scripts for setup the environment

### Dependencies

```
$ brew install yarn
$ brew install jq
$ brew install wget
```

### Run

Copy script to directory and run:

```
$ sh server.sh
$ sh react.sh
```
